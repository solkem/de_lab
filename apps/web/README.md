# LakeForge Web

## Run locally

```powershell
npm install
npm run dev
```

The current UI is a Phase 1 workspace shell that mirrors the product areas we will build next.

Notebook workbench route:

- `/notebooks`
- `/jobs`
- `/pipelines`
- `/lineage`
